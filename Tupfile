
.gitignore

compiler = c++
cpp_flags = -std=c++11 -I.
link_flags = -std=c++11 -lSDL2

: foreach *.cc |> $(compiler) $(cpp_flags) -c %f -o %o |> %B.o {objs}
: {objs} |> $(compiler) $(link_flags) %f -o %o |> main

